# Infraestructura

Esta carpeta almacenará configuraciones de infraestructura reproducibles.

```text
infrastructure/
├── docker/      # Dockerfiles y archivos relacionados
└── database/    # Inicialización y herramientas de PostgreSQL
```

Las credenciales y datos persistentes locales nunca deben confirmarse en Git.
